# API URL Configuration
# Leave this as /api to use Vite's proxy in development
# This works for both local development and ngrok tunnels
VITE_API_URL=/api

# Force external testing mode
# Set to 'true' to disable all localhost connections
VITE_FORCE_EXTERNAL_MODE=false